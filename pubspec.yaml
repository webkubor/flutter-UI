name: sms_reader_app
description: A simple welcome page app

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  device_info_plus: ^9.0.0
  webview_flutter: ^4.10.0
  webview_flutter_android: ^4.0.0+1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  
  assets:
    - assets/images/
    - assets/fonts/
